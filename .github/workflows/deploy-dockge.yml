name: Deploy Dockge

on:
  push:
    branches:
      - main
    paths:
      - 'dockge/**'

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Fetch Latest Code & Deploy
        run: |
          cd /home/cargo/docker/dockge 

          git fetch --all
          git reset --hard origin/main
          
          docker compose up -d --remove-orphans
