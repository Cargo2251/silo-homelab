name: Deploy Dozzle

on:
  push:
    branches:
      - main
    paths:
      - 'dozzle/**'

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Fetch Latest Code & Deploy
        run: |
          cd /home/cargo/docker/dozzle

          git fetch --all
          git reset --hard origin/main
          
          docker compose up -d --remove-orphans
