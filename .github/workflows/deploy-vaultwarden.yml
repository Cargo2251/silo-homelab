name: Deploy Vaultwarden

on:
  push:
    branches:
      - main
    paths:
      - 'vaultwarden/**'

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Fetch Latest Code & Deploy
        run: |
          cd /home/cargo/docker/vaultwarden

          git fetch --all
          git reset --hard origin/main
          
          docker compose up -d --remove-orphans
