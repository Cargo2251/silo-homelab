services:
  bot:
    image: python:3.12-slim
    volumes:
      - ./app:/app
      working_dir: /app
      command: /bin/sh -c "pip install --no-cache-dir -r requirements.txt && python bot.py
      restart: unless-stopped"
